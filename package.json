{
  "name": "pdfnectar-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist",
    "build:dev": "cd frontend && npm install && npm run build:dev && cd .. && rm -rf dist && cp -r frontend/dist dist",
    "preview": "cd frontend && npm run preview"
  }
}
